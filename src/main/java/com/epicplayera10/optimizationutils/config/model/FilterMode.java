package com.epicplayera10.optimizationutils.config.model;

public enum FilterMode {
    /**
     * Only entities matching the list are affected.
     */
    INCLUDE,
    /**
     * All entities except those matching the list are affected.
     */
    EXCLUDE
}
